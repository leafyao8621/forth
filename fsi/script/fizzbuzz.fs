: fizzbuzz
    0 do
        0
        i 3 mod 0= if
            s" Fizz" type
            1+
        then
        i 5 mod 0= if
            s" Buzz" type
            1+
        then
        0= if
            i .
        then
        cr
    loop
;
